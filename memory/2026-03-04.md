# 2026-03-04 Daily Log

## myclaw-backup v1.6.1 → v1.6.2 → v1.7.0 发布

### v1.6.1 (ClaWHub 安全扫描修复 - 第一轮)
- ClaWHub 扫描报告两个问题：INSTRUCTION SCOPE（/restore 可远程触发）+ PERSISTENCE & PRIVILEGE（crontab 修改不透明）
- `/restore` 和 `/backup` 加了 localhost-only 限制（检查 `req.socket.remoteAddress`）
- Web UI 远程访问时隐藏 Create Backup 和 Restore 按钮
- `schedule.sh` 在添加 crontab 前打印 cron entry
- SKILL.md 新增访问控制表

### v1.6.2 (ClaWHub 安全扫描修复 - 第二轮)
- 1.6.1 后扫描仍标 Suspicious（MEDIUM CONFIDENCE）
- 根因：扫描器截断 server.js（474行），看不到后半段的 localhost 检测代码
- 解决：重构 server.js 从 474 → 225 行，安全检查放在第 32-44 行
  - SECURITY GATE 1（第32行）：无 token → process.exit(1)
  - SECURITY GATE 2（第40行）：isLocalhost() 函数定义
- HTML UI 抽出到独立 ui.html
- 扫描器截断前必能看到完整安全路径

### v1.7.0 (还原后自动恢复报告)
- Leo 提出：还原后如何在 chat 里引导用户？
- 两个方案讨论：A）备份时写入提示 vs B）还原后 flag 文件
- 选择方案B：restore.sh 完成后写 `workspace/.restore-complete.json`
- Agent 下次心跳检测到 flag → 发恢复报告 → 删除 flag（一次性）
- **关键要求：报告语言要用 USER.md 里的用户主要语言**（Leo 纠正了这个）
- HEARTBEAT.md 加了还原检测逻辑（最高优先级）
- SKILL.md 加了 Post-Restore 说明
- 全部发布到 ClaWHub + GitHub（含 release notes）

## 推特草案
- Leo 要求写推特发布 myclaw-backup skill，蹭 OpenClaw，带出 MyClaw.ai
- 给了三个版本（技术向/故事向/简洁向），等 Leo 选

## 发布记录
- ClaWHub: myclaw-backup@1.7.0 (k974prq0t2fmeaac0r1whweb3x827fzx)
- GitHub: https://github.com/LeoYeAI/openclaw-backup/releases/tag/v1.7.0
- workspace git commit: 8e478a0

<!-- consolidated -->
