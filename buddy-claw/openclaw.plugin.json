{
  "id": "buddy-claw",
  "name": "Buddy Claw",
  "description": "Virtual pet companion that lives in your chat — reacts to conversations, grows with interaction",
  "uiHints": {
    "enabled": {
      "label": "Enabled",
      "help": "Enable or disable the buddy companion"
    },
    "reactChance": {
      "label": "Reaction Chance",
      "help": "Probability (0.0–1.0) that buddy reacts after each conversation turn"
    },
    "silentReactions": {
      "label": "Silent Reactions",
      "help": "Send reactions without notification sound"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "reactChance": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      },
      "silentReactions": {
        "type": "boolean"
      },
      "dbPath": {
        "type": "string"
      }
    }
  }
}
